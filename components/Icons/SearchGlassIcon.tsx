import { Icon } from "@/Types/Icon";
import { HiMagnifyingGlass } from "react-icons/hi2";

export default function SearchGlassIcon({ size, color }: Icon) {
  return <HiMagnifyingGlass size={size} color={color} />;
}
